import { PageLoading } from "@/components/page-loading";

// The route group keeps the shell interactive while any child route streams.
export default function Loading() {
  return <PageLoading />;
}
