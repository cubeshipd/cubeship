# Dashboard screenshots

Captured from the real dashboard in preview mode (`make dashboard-preview`),
using demonstration data, a 1600 × 1100 viewport, device scale factor 2, dark
colour scheme and reduced motion. Wait for fonts, data and charts before capture.
All three images use the same dimensions so the showcase does not jump between tabs.

- `overview.png`: `/`
- `app.png`: `/projects/web/production/api`
- `backups.png`: `/backups`

Refresh these images when changing the dashboard's visual design.
