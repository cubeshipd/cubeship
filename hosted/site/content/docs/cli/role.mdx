---
title: cubeship role
description: "Read the access roles members and API keys are given"
---

{/* Generated from the CLI by `cubeship docs` — run `make reference`. Editing this file is editing the copy rather than the thing. */}

Read the access roles members and API keys are given

## cubeship role list

List the access roles on this instance. Each grant is a kind of
resource, a level (view or manage), whether it reads secrets —
variables, credentials, files — and which items, or every one.

```bash
cubeship role list
```

