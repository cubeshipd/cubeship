# Everything the build does not read. The Go and web sources are copied
# explicitly; what is left here is output, caches and local state that
# would only invalidate the layer cache.
.git
product/.air
bin
dist
product/dashboard/node_modules
product/dashboard/.next
site/node_modules
site/.next
site/.source
coverage.out
